//Modularization using common.js library
//This will definitely help with creating functions, data, codes, JSON objects, etc, when going through our final project


var loadFile = require("newFile");
//var newFile = require("blerchFile");

loadFile.runMessage();

//console.log(loadFile.myMessage);

//Referencing a JSON object
//for(var i in loadfile.json);
